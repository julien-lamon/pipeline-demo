export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-surface">
      <div className="mx-auto max-w-5xl px-4 py-6 text-xs text-muted">
        <p className="font-medium text-foreground">Pipeline · démo vitrine</p>
        <p className="mt-1">
          Données 100% fictives et figées. Aucun appel IA, aucun secret, aucune
          donnée réelle. Le coaching live (vrai modèle, génération de CV) arrive
          dans une version ultérieure.
        </p>
      </div>
    </footer>
  );
}
